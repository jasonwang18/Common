<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supcon.common">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".TextApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        >
        <activity android:name=".ui.RouterTestActivity2"/>
        <activity android:name="com.supcon.common.MainActivity" android:theme="@style/AppNoTitleBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RouterTestActivity" android:theme="@style/AppNoTitleBarTheme"/>
        <activity android:name=".ui.WebViewTestActivity" android:theme="@style/AppNoTitleBarTheme"/>
    </application>

</manifest>
